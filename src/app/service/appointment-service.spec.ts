import { AppointmentService } from './appointment-service';

describe('AppointmentService', () => {
  it('should create an instance', () => {
    expect(new AppointmentService()).toBeTruthy();
  });
});
