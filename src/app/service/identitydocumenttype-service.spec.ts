import { IdentitydocumenttypeService } from './identitydocumenttype-service';

describe('IdentitydocumenttypeService', () => {
  it('should create an instance', () => {
    expect(new IdentitydocumenttypeService()).toBeTruthy();
  });
});
